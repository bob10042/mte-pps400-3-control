<UserControl x:Class="MtePpsControl.Views.RippleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             >
    <UserControl.Resources>
        <Style TargetType="RadioButton" x:Key="ChipRadio">
            <Setter Property="Background" Value="{StaticResource BgRaised}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="brd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="brd" Property="Background" Value="{StaticResource AccentSoft}"/>
                                <Setter TargetName="brd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                <Setter Property="Foreground" Value="{StaticResource Text}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Ripple control telegram" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Foreground="{StaticResource TextDim}" FontSize="12"
                       Text="Sequence of pulses/pauses. Push pre-loads the telegram; SET on Source Control fires it. Up to 250 steps per phase."/>
        </StackPanel>

        <!-- Phase + V/I + abs/% selector -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Phase:" Margin="0,4,8,0" Foreground="{StaticResource TextDim}"/>
            <RadioButton Content="L1" Style="{StaticResource ChipRadio}" GroupName="rPhase" IsChecked="{Binding IsPhase1}" Command="{Binding SelectPhase1Command}"/>
            <RadioButton Content="L2" Style="{StaticResource ChipRadio}" GroupName="rPhase" IsChecked="{Binding IsPhase2}" Command="{Binding SelectPhase2Command}"/>
            <RadioButton Content="L3" Style="{StaticResource ChipRadio}" GroupName="rPhase" IsChecked="{Binding IsPhase3}" Command="{Binding SelectPhase3Command}"/>

            <TextBlock Text="Side:" Margin="20,4,8,0" Foreground="{StaticResource TextDim}"/>
            <RadioButton Content="Voltage (RCSU)" Style="{StaticResource ChipRadio}" GroupName="rSide" IsChecked="{Binding IsVoltage}" Command="{Binding SelectVoltageCommand}"/>
            <RadioButton Content="Current (RCSI)" Style="{StaticResource ChipRadio}" GroupName="rSide" IsChecked="{Binding IsCurrent}" Command="{Binding SelectCurrentCommand}"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Amplitude:" Margin="0,4,8,0" Foreground="{StaticResource TextDim}"/>
            <RadioButton Content="Absolute (A/V)" Style="{StaticResource ChipRadio}" GroupName="rUnit" IsChecked="{Binding IsAbsolute}" Command="{Binding SelectAbsoluteCommand}"/>
            <RadioButton Content="% of fundamental" Style="{StaticResource ChipRadio}" GroupName="rUnit" IsChecked="{Binding IsPercent}" Command="{Binding SelectPercentCommand}"/>

            <TextBlock Text="{Binding Steps.Count, StringFormat=Steps: {0}/250}" Margin="24,4,0,0" Foreground="{StaticResource TextDim}"/>
        </StackPanel>

        <!-- Steps table -->
        <Border Grid.Row="3" Background="{StaticResource BgPanel}"
                BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="3" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="12,8" Background="{StaticResource BgRaised}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="#" Margin="8,4" Foreground="{StaticResource TextDim}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Duration (ms)" Margin="8,4" Foreground="{StaticResource TextDim}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="Amplitude" Margin="8,4" Foreground="{StaticResource TextDim}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="Frequency (Hz)" Margin="8,4" Foreground="{StaticResource TextDim}" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="4" Text="Type" Margin="8,4" Foreground="{StaticResource TextDim}" FontWeight="SemiBold"/>
                </Grid>
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Steps}" AlternationCount="2">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="12,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="60"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" FontFamily="Consolas" Margin="8,4" VerticalAlignment="Center"
                                               Text="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, StringFormat={}{0}}"/>
                                    <TextBox Grid.Column="1" Margin="8,2" Text="{Binding DurationMs, UpdateSourceTrigger=PropertyChanged, StringFormat=F1}"/>
                                    <TextBox Grid.Column="2" Margin="8,2" Text="{Binding Amplitude, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>
                                    <TextBox Grid.Column="3" Margin="8,2" Text="{Binding FrequencyHz, UpdateSourceTrigger=PropertyChanged, StringFormat=F1}"/>
                                    <TextBlock Grid.Column="4" Margin="8,4" VerticalAlignment="Center" FontFamily="Consolas">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="pause"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextDim}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPulse}" Value="True">
                                                        <Setter Property="Text" Value="pulse"/>
                                                        <Setter Property="Foreground" Value="{StaticResource Accent}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Action row -->
        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <Button Content="+ Step"   Style="{StaticResource PanelButton}" Command="{Binding AddStepCommand}"      Margin="0,0,4,0"/>
                <Button Content="Duplicate" Style="{StaticResource PanelButton}" Command="{Binding DuplicateLastCommand}" Margin="0,0,4,0"/>
                <Button Content="− Last"   Style="{StaticResource PanelButton}" Command="{Binding RemoveStepCommand}"   Margin="0,0,4,0"/>
                <Button Content="Clear"    Style="{StaticResource PanelButton}" Command="{Binding ClearStepsCommand}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Stop telegram" Style="{StaticResource WarnButton}"    Command="{Binding StopCommand}" Margin="0,0,8,0"/>
                <Button Content="Push to PPS"   Style="{StaticResource PrimaryButton}" Command="{Binding PushCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
