<UserControl x:Class="MtePpsControl.Views.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding Database, RelativeSource={RelativeSource AncestorType=Window}}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Saved presets" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Foreground="{StaticResource TextDim}" FontSize="12"
                       Text="Snapshot the source-control setpoints (U/I/PH/φ/ranges/SKLI/FRQ) under a name. Stored in %APPDATA%\MtePpsControl\presets.json."/>
        </StackPanel>

        <!-- Save row -->
        <Border Grid.Row="1" Margin="0,0,0,12" Background="{StaticResource BgPanel}"
                BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="3" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Name:" VerticalAlignment="Center" Foreground="{StaticResource TextDim}" Margin="0,0,8,0"/>
                    <TextBox Width="280" Text="{Binding NewPresetName, UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Left"/>
                </StackPanel>
                <Button Grid.Column="1" Content="Save current state" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}"/>
            </Grid>
        </Border>

        <!-- Presets list -->
        <Border Grid.Row="2" Background="{StaticResource BgPanel}"
                BorderBrush="{StaticResource BorderBrush1}" BorderThickness="1" CornerRadius="3">
            <ListBox ItemsSource="{Binding Presets}" SelectedItem="{Binding SelectedPreset}"
                     Background="Transparent" BorderThickness="0">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource Text}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="brd" Background="{TemplateBinding Background}" Padding="0">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="brd" Property="Background" Value="{StaticResource AccentSoft}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="12,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                <TextBlock FontFamily="Consolas" Foreground="{StaticResource TextDim}" FontSize="11">
                                    <Run Text="U=["/><Run Text="{Binding U[0], StringFormat=F0}"/><Run Text=","/><Run Text="{Binding U[1], StringFormat=F0}"/><Run Text=","/><Run Text="{Binding U[2], StringFormat=F0}"/><Run Text="]V  "/>
                                    <Run Text="I=["/><Run Text="{Binding I[0], StringFormat=F2}"/><Run Text=","/><Run Text="{Binding I[1], StringFormat=F2}"/><Run Text=","/><Run Text="{Binding I[2], StringFormat=F2}"/><Run Text="]A  "/>
                                    <Run Text="{Binding Frequency, StringFormat=F0}"/><Run Text="Hz"/>
                                </TextBlock>
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding SavedAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}"
                                       FontFamily="Consolas" Foreground="{StaticResource TextDim}" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Delete" Style="{StaticResource WarnButton}"    Command="{Binding DeleteCommand}" Margin="0,0,8,0"/>
            <Button Content="Load"   Style="{StaticResource PrimaryButton}" Command="{Binding LoadCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
